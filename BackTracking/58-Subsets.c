#include<stdio.h>

void printSubsets(int allSubsets[]) {
    for(int i = 0; sizeof(allSubsets)/sizeof(allSubsets[0]); i++) {
        for (int j = 0; i < sizeof(allSubsets[i]) / sizeof(allSubsets[i]); i++) {}
    }
}

int main() {
    int nums[] = {1,2,3};
    int ans[sizeof(nums)/sizeof(nums[0])];
    int allSubsets[(sizeof(nums) / sizeof(nums[0]))];
}